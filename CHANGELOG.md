# 更新日志

本文记录 `logdns/imyemail` 维护分支的主要用户可见变化。完整提交和构建产物见 [GitHub Releases](https://github.com/logdns/imyemail/releases)。

## v1.3.23

- 继续简化“通知与客户端”配置区：桌面端使用自适应表格、窄屏使用卡片，鉴权说明集中为简明的登录信息提示，服务器、端口和用户名保留独立复制按钮。
- 将前台配额和后台配额设置中的 IMAP/POP3 计数单位统一为“请求”，并同步简体中文、繁體中文和 English 文案。

## v1.3.22

- 重做“通知与客户端”配置区：桌面端使用清晰表格、移动端使用卡片，完整展示 IMAP 993、POP3 995、SMTP 465 与 Submission 587，并为服务器、端口和用户名提供独立复制按钮。
- 将前台反馈从浏览器本地记录升级为 SQLite 工单：用户与管理员可双向回复、查看同步状态、关闭和经二次确认删除工单；用户归属及后台查看/管理权限均由 Go API 强制校验。
- 工单标题和正文按纯文本显示，限制长度与请求体大小，并使用独立持久化事件限制创建和回复频率，避免通过删除工单绕过限流。
- 新增客户端配置、用户工单与后台工单的稳定 UI 契约，并补齐简体中文、繁體中文和 English 文案。

## v1.3.21

- `imyemail-vbena` 登录页左侧新增以 `@` 为核心的邮箱品牌视觉，保持移动端单栏、深色主题和减少动态效果兼容，不改变认证流程。
- 登录、注册、Webmail 和个人中心现在都提供用户可操作的语言选择入口；用户选择继续保存在浏览器本地，并优先于系统默认语言。

## v1.3.20

- 优化后台“关于”信息，展示 imyemail 品牌、产品说明、当前版本、联系邮箱和 `https://imy.email/` 官网入口。
- 重构系统版本弹窗的固定头部、滚动内容区和底部操作区，改善移动端及长更新日志场景的对齐与可操作性。
- 超级管理员可在二次确认后删除上一版本的旧镜像和 Compose 回滚文件；当前版本、SQLite、Maildir、附件、证书和 DKIM 数据不受影响。删除操作同时受后台角色、内部令牌和系统排他锁保护。
- 优化 `imyemail-vbena` Webmail 左侧栏，增加使用站点名称并回退到 `imyemail` 的品牌区，兼容简体中文、繁体中文、英文、浅色和深色界面。

## v1.3.19

- 新增 `imyemail-vbena` 前后台界面模板，参考 Vben Admin 5.7 的蓝色品牌 token、224px 侧栏、50px 固定顶栏、圆角导航和模块化内容布局，覆盖登录、注册、Webmail、个人中心与管理后台。
- 新模板支持简体中文、繁体中文和英文，以及 375/768/1440/2560 宽度、浅色/深色和减少动态效果模式；宽屏自适应扩展导航与邮件列表。
- 前后端固定白名单、公开设置、管理后台预览、持久化测试和模板契约同步扩展；模板仅改变受控视觉层，不改变认证、授权或邮件数据接口。

## v1.3.18

- 新增 `imyemail-cloud-sy` 前后台界面模板，参考 SoybeanAdmin 的主题 token、纵向侧栏、56px 顶栏和分层内容布局，覆盖登录、注册、Webmail、个人中心与管理后台。
- 新模板支持 375/768/1440/2560 宽度、浅色/深色及减少动态效果模式；宽屏自适应扩展导航与邮件列表，不限制业务内容宽度。
- 前后端同步扩展模板固定白名单、公开设置、管理后台预览、三语言文案和契约测试，不改变认证、授权或邮件数据接口。

## v1.3.17

- 安装完成后统一输出 Webmail/管理后台地址、初始管理员用户名、密码安全获取方式和持久化数据目录，减少遗漏初始登录信息的情况。
- 自动生成的管理员密码继续只写入权限为 `0600` 的本地文件，最终摘要只提示安全查看命令，不把明文密码写入终端日志。

## v1.3.16

- 修复 English 与繁體中文界面中写信正文、签名、自动回复和反馈表单仍显示简体中文 placeholder 的问题。
- 语言同步现在只保护 textarea 与富文本编辑器中的用户内容，同时翻译其界面属性；写信正文提示由编辑器按当前语言原生生成，切换语言不会修改邮件正文。
- 扩展三语言构建检查，持续验证 textarea 与富文本 placeholder 的本地化契约。

## v1.3.15

- 管理后台“系统设置 → 基础”新增全站默认语言，支持简体中文、繁體中文和 English。
- 登录、注册、Webmail、个人中心和管理后台共用同一语言配置；用户主动选择的语言继续优先于管理员默认值。
- 服务端对语言值执行固定白名单校验，并通过公开设置安全下发；新增三语言界面翻译完整性检查。

## v1.3.14

- 优化 `imyemailcloud` 桌面与超宽屏布局，移除 Webmail 和个人中心固定外框宽度，减少无效留白。
- 宽屏 Webmail 自适应扩展导航、邮件列表和详情区域，同时保持移动端、深色模式和减少动态效果兼容。
- 新增界面模板契约检查以及统一的设计、融合、验证、测试、安全审计、文档和发布完成标准。

## v1.3.13

- 管理后台新增“界面模板”，可在全站切换 `imyemaildefault` 与 `imyemailcloud`。
- 新增云端轻盈风格的登录、Webmail、个人中心和管理后台视觉模板，并保持移动端与深色模式兼容。
- 模板设置持久化到系统设置，通过公开配置统一下发；后端只接受内置模板白名单。

## v1.3.12

- 修复后台在线更新过程中 API 容器被重启，浏览器显示 `Failed to fetch` 的竞态问题。
- 新增独立 Operator 内部运维服务；更新请求先返回 `202`，再创建备份与回滚点并异步更新。
- 后台系统版本弹窗新增上一版本、回滚点时间、二次确认和回滚按钮。
- 回滚前再次备份 SQLite，并明确镜像/Compose 回滚不会回退数据库。
- 新增 `ghcr.io/logdns/imyemail-operator` 的 amd64/arm64 GHCR 镜像和发布校验。

## v1.3.11

- 前台新增 SMTP、IMAP、POP3 客户端连接历史，支持选择和批量删除。
- 管理后台数据总览新增邮件收发、账号、附件、队列、客户端连接和存储使用统计。
- 写信窗口新增“我的图库”，支持 `tu.my` 或兼容 HTTPS 上传接口。
- 更新相关 API、架构、客户端和运维文档。

## v1.3.10

- 同步正式发布静态文件、Release 和多架构镜像，无新增业务功能。

## v1.3.9

- 修复 Dovecot 鉴权策略上报，补齐 IMAP/POP3 客户端连接记录。

## v1.3.8

- 移除未接入真实审计数据的占位安全活动，避免把模拟内容显示为真实记录。

## v1.3.7

- 修复 SMTP Submission、IMAP 和 POP3 客户端兼容性。
- SMTP Submission 支持 `AUTH PLAIN` 与 `AUTH LOGIN`。
- 新增真实客户端鉴权审计入口，并强化记录的用户与邮箱归属校验。
- 改进 2FA 与应用密码在第三方客户端中的验证流程。

## v1.3.6

- 修正文档和界面对附件上限、邮箱密码与 2FA 应用密码的说明。

## v1.3.5

- 修复 MIME 中文正文解码，兼容 GB18030、GBK、GB2312 等常见 charset。
- 新增每邮箱附件上限，并与账号权限配额统一校验。
- 新增标准 TOTP 扫码、恢复码和每邮箱第三方客户端应用密码。
- 新增全域公告发布、更新和下线。
- 完善备份、恢复、迁移、TOTP 时间同步和邮件客户端文档。

## v1.3.4

- 管理后台“全部邮件”新增查看、筛选、选择和批量管理。
- 管理后台“发送队列”新增筛选、投递审计、批量重试、取消和删除。
- 改进前台退出登录、邮箱切换、管理员入口和站点标题设置。
- 正式静态资源使用版本固定的 jsDelivr 地址，并保留本地回退。

## v1.3.3

- 完善登录后的账号入口、站点设置和邮件收发链路。
- 完善单容器 Postfix、Submission、Maildir 同步和部署环境配置。

## v1.3.2

- 支持使用已绑定的完整邮箱地址登录 Webmail，同时保留登录名登录。
